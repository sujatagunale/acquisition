# Staging environment variables
NODE_ENV=staging
PORT=3000
# Staging DB (can be separate Neon branch)
DATABASE_URL=
# Staging JWT secret
JWT_SECRET=
# Allow broader origins for staging if needed
CORS_ORIGIN=*
